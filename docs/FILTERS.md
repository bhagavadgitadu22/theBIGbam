Several categories of filters exist: the Contig filters allows to only consider contigs bigger than a certain length or with more than certain fraction of duplication.

Each module calculates metrics per present pair
